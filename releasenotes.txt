0.7.7.0

Add six languages: Italian, Korean, Polish, Dutch, Turkish, and Ukrainian.
Language options menu lists all fourteen languages with tighter row spacing.
Korean uses Noto Sans KR when available (MartianMono fallback). Ukrainian stays on MartianMono (covers ї є ґ).

0.7.6.1

Steam beta ship: languages (0.7.5) + theme-colored particles (0.7.6) with build digit rolled for this upload.
APPLICATION_VER is Major.Minor.Feature.Build — roll the 4th digit on every ship/upload.

0.7.6.0

Theme-colored particle bursts on line clear (primary), hard drop / piece lock (medium), and combo / Tetris+ (stronger on 4+ lines).
Cap 160, pause freezes the pool, jazz and ablockalypse palettes. F11 and ABLOCKALYPSE_PARTICLES remain as debug toggles.
No ambient board dust.

0.7.5.0

Add six languages: French, German, Portuguese (Brazil), Russian, Japanese, and Simplified Chinese.
Language options menu is data-driven for all eight languages.
Japanese and Simplified Chinese use Noto Sans CJK fonts when available (MartianMono fallback).

0.7.4.0

Options Theme switcher: jazz (original) vs ablockalypse (new), music + visuals.
Full 9-track jazz playlist under assets/audio/music.

0.7.1

Add UI elements to allow the player to remap the keyboard and gamepad in the input options screen.
Add an option to start on a specific level.
Fix an issue where when wall kick was enabled it would only work for counter clockwise.
Add an option to allow the player to choose the start level.

0.7.0.6

Add option for quick drop cool down which prevents accidental drop.
Can remap the gamepad buttons with a file (ui and keyboard comming soon).
Fix issue with double esc or double enter when navigating screens.
Add additional levels going up to 31 with a 10ms gravity.

0.7.0.4

Removed the cats.
Added new block textures.
New Ablockalypse logo.
New main menu graphcis.

0.7.0.3

Blockeditor.
Ensure blocks are regenerated when exiting and starting the game.
Ensure the y axis is correct for blocks put into play regardless of shape or size so that the bottom of the block lines up with the top of the board.

0.6.7.0

Wallkick and wallkick options

0.6.5.4

The gamepad now uses the user option for input delay and repeat to match how the keyboard works.
Add the guidegrid option which allows for different colors.
Add localization to the Board Size, Input Delay Input Repeat and Guide Grid labels in the options.

0.6.5.3

Update the rotations slightly to be more intuitive.

0.6.5.1

Add a transparent layer between the blocks and background so that the blocks are easier to see against the background.

0.6.5.0

Added competly new beautiful set of backgrounds.

->.

Fix bug where the combo score wasn't cleared on a new game.
Fix bug where the language preference wasn't saved and loaded on the next run.
Fix bug where the curser wasn't pulsing when entering a high score.
Fix bug where background may not show after an update on Linux.

0.6.3.04

Added Spanish language selection.
Use blend mode for font which renders a higher quality font.
